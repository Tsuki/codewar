/**
 * @param {number[]} A
 * @param {number} S
 * @return {number}
 */
const numSubarraysWithSum = function (A, S) {

};

const assert = require('assert');
describe('Array', function () {
    describe('numSubarraysWithSum', function () {
        it('1', function () {
            assert.equal(numSubarraysWithSum([1, 0, 1, 0, 1], 2), 4);
        });
    });
});